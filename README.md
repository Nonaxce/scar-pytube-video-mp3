## scar-pytube-video-mp3

No longer works for someone reason (it's probably the library or me)

**for my own and limited use because very unintuitive**

### How to use

Prerequisites: `python`, `pip`

Install `pytube` using pip with terminal
`pip install pytube`

Download `main.py` file

To run, open terminal and run the following
`C:\Users\YOURUSER\YOURPATH python main.py`

`YOURPATH` is the level after `YOURUSER` directory, such as `Downloads`




Still no UI
2025 update: still no ui 


 
